# Bản sao thành .env.local — chỉ dùng khi test gọi API ngoài localhost
# Ví dụ backend public: https://ten-mien.com hoặc tunnel (ngrok) — phải kết thúc bằng /api
# VITE_API_BASE_URL=https://api.example.com/api
